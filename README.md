# RSSCHOL NodeJS 2022 Q4, WebSocket app remote control

## Installation

1. Clone repo
2. Install dependencies - `npm i`

## Usage

**Dev mode**

`npm run start:dev`

This command runnig nodemon for WebSocket server and node for frontend `http://localhost:8181`

`npm run start:dev`

Run server without nodemon
